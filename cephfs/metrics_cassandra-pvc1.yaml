apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metrics-cassandra-1
  namespace: openshift-infra
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  volumeName: cassandra-storage-1
